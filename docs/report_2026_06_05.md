# Báo cáo Tiến độ Phát triển AuraFit — Ngày 05/06/2026 (Cập nhật Tái cấu trúc & Tap Nav)

Hôm nay chúng ta đã thực hiện tái cấu trúc lại thư mục nguồn theo mô hình Feature-based chuyên nghiệp, khôi phục luồng điều hướng Tap-to-Switch, tích hợp nhãn Tooltip điều hướng động trên BottomNav, và phác thảo thiết kế Backend/Database cho giai đoạn phát triển tiếp theo.

---

## 🛠️ Danh sách các công việc đã thực hiện (Accomplishments)

### 1. Tái cấu trúc thư mục theo mô hình Feature-based độc lập
- **Phân tách Feature**: Đã phân tách mã nguồn thành các feature con độc lập nằm trong [src/features/](file:///c:/Projects/Personal/aurafit/mobile/src/features/):
  - **`dashboard`**: Chứa trang chủ Landing Page [HomeScreen.tsx](file:///c:/Projects/Personal/aurafit/mobile/src/features/dashboard/screens/HomeScreen.tsx) và các component giao diện (Header, Bento, Hero, Quote, Inquiry, BottomNav).
  - **`workout`**: Chứa màn hình quản lý bài tập [WorkoutScreen.tsx](file:///c:/Projects/Personal/aurafit/mobile/src/features/workout/screens/WorkoutScreen.tsx).
  - **`leaderboard`**: Chứa màn hình bảng xếp hạng [LeaderboardScreen.tsx](file:///c:/Projects/Personal/aurafit/mobile/src/features/leaderboard/screens/LeaderboardScreen.tsx).
  - **`profile`**: Chứa màn hình hồ sơ cá nhân [ProfileScreen.tsx](file:///c:/Projects/Personal/aurafit/mobile/src/features/profile/screens/ProfileScreen.tsx).
- **Dọn dẹp**: Đã xóa bỏ hoàn toàn các tệp screen placeholder thừa dưới thư mục `dashboard/screens/` để giữ thư mục nguồn sạch sẽ.

### 2. Thiết lập Điều hướng chuyển trang Tap-to-Switch
- **Cơ chế**: Gỡ bỏ cử chỉ PanResponder trên BottomNav và horizontal ScrollView trượt ngang toàn màn hình. 
- **Lợi ích**: Chuyển sang render có điều kiện (conditional rendering) tại [DashboardScreen.tsx](file:///c:/Projects/Personal/aurafit/mobile/src/features/dashboard/screens/DashboardScreen.tsx) giúp việc chuyển trang qua nhấn tab diễn ra tức thời và ổn định 100% trên mọi thiết bị di động.

### 3. Nhãn Tooltip động trên BottomNav
- **Hoạt động**: Thêm các Capsule Tooltip nhỏ nổi lên trên mỗi nút tab khi hoạt động (`absolute -top-7`).
- **Hiển thị**: Khi tab được kích hoạt, nhãn chữ tương ứng ("HOME", "WORKOUT", "LEADER", "PROFILE") sẽ tự động hiển thị để người dùng nhận diện nhanh trang hiện tại.

### 4. Đề xuất Thiết kế Backend & Database (BE & DB Concept)
- **Giải pháp Serverless + Supabase (Khuyến nghị)**: Tận dụng hệ sinh thái Supabase (API REST, Authentication qua RLS PostgreSQL, Realtime WebSockets, Storage) kết hợp **Supabase Edge Functions** (TypeScript/Deno) để triển khai nhanh chóng và tiết kiệm chi phí.
- **Node.js Backend**: Tạo thư mục `backend/` song song trong monorepo (`c:\Projects\Personal\aurafit\backend`) sử dụng Express hoặc NestJS nếu có nhu cầu xử lý nặng phía server.
- **Sơ đồ DB mẫu**: Phác thảo cấu trúc các bảng `users`, `exercises` (bài tập gốc), `workout_templates` (giáo án), `workout_logs` & `set_logs` (nhật ký tập), `muscle_fatigue` (chỉ số mỏi cơ).

---

## 📂 Danh sách các tệp đã thêm mới/sửa đổi (Files Log)

| Trạng thái | Tên tệp | Vai trò |
| :--- | :--- | :--- |
| **[NEW]** | [HomeScreen.tsx](file:///c:/Projects/Personal/aurafit/mobile/src/features/dashboard/screens/HomeScreen.tsx) | Trang chủ cuộn chứa nội dung Landing Page |
| **[NEW]** | [WorkoutScreen.tsx](file:///c:/Projects/Personal/aurafit/mobile/src/features/workout/screens/WorkoutScreen.tsx) | Trang tập luyện trong feature `workout` |
| **[NEW]** | [LeaderboardScreen.tsx](file:///c:/Projects/Personal/aurafit/mobile/src/features/leaderboard/screens/LeaderboardScreen.tsx) | Trang xếp hạng trong feature `leaderboard` |
| **[NEW]** | [ProfileScreen.tsx](file:///c:/Projects/Personal/aurafit/mobile/src/features/profile/screens/ProfileScreen.tsx) | Trang cá nhân trong feature `profile` |
| **[MODIFY]** | [DashboardScreen.tsx](file:///c:/Projects/Personal/aurafit/mobile/src/features/dashboard/screens/DashboardScreen.tsx) | Điều phối chuyển đổi conditional rendering |
| **[MODIFY]** | [BottomNav.tsx](file:///c:/Projects/Personal/aurafit/mobile/src/features/dashboard/components/BottomNav.tsx) | Gỡ bỏ PanResponder, thêm Tooltip nổi trên tab |

---

## 📈 Kết quả Xác minh (Verification Output)
- Đã chạy kiểm thử TypeScript: Bộ biên dịch TS không phát hiện lỗi nào (`exit code: 0`). Giao diện hoạt động cực kỳ mượt mà, ổn định và sẵn sàng chạy thử.
